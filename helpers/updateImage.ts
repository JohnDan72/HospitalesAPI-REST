import Usuario from "../models/usuario";
import Hospital from "../models/hospital";
import Medico from "../models/medico";

export const updateImagen = (tipo: string, uid: string, path: string, fileName: string) => {
    switch (tipo) {
        case 'usuarios':

            break;
        case 'hospitales':

            break;
        case 'medicos':
            // const medico = await Medico.findBy
            break;

        default:
            break;
    }
}